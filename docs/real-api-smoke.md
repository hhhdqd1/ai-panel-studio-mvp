# 真实 DeepSeek API 冒烟记录

时间：2026-09-23（本地）；环境：macOS，本地临时 SQLite 数据库；网关：`DeepSeekGateway`，模型环境变量未覆盖时为 `deepseek-chat`。使用 `python -m app.smoke --real --topic '中小学应如何稳妥试点 AI 学习助手？' --experts 2`。`--real` 是明确的计费调用开关；运行时已有本机环境 Key，但本记录、日志和仓库没有记录 Key 值。脚本默认用临时目录，完成后删除此次数据库。

| 指标 | 真实结果 |
| --- | --- |
| 最终状态 | `completed`，无错误码 |
| 总耗时 | 244.6 秒 |
| 模型调用 | 54 次 |
| 阵容 | 1 位主持人、2 位专家；共 3 人 |
| 发言 | 16 条，其中专家回合 12 条 |
| 风险标记 | 2 条待核实断言 |
| 总结 | 生成自然语言总结 |

人工阅读总结：讨论围绕试点效果指标、预先约定的停止条件以及换实施路径后的判断边界展开；总结能区分已经趋同的判断和仍有争议的问题，没有把整场讨论压成一串 JSON。总结提到“0.3 个标准差”等数字时，也明确指出缺少来源、适用条件或功效计算，仍需外部核实。这个观察只说明此轮输出有可读的结构与风险提示，不代表相关教育结论已经被核查为真。

费用边界：54 次请求可能产生 API 费用，金额取决于账号计费和实际 token 用量；当前 `model_run` 记录请求目的/耗时/错误码，但没有完整逐请求 token 账单，因此不报未经核实的金额。演示与自动化回归默认使用 `APP_FAKE_MODEL=1`，不要重复运行 `--real` 来做普通 UI 测试。真实模型速度与输出随服务、时间和采样而变化，本次结果不是性能保证。

后续于 2026-09-24 完成快照一致性、六专家预算与审查失败提示的代码审查修复；这些变更已用假网关和浏览器回归测试验证，没有再次进行可计费的真实 API 烟测。上表只记录 2026-09-23 那一次实际调用结果。
